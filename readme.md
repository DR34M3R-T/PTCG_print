# A Printable PTCG Card Layout Generator

1.  First, you must have Python installed.   
2.  Next, using  
    ```bash
    pip install -r requirements.txt
    ```
    to install required packages.  
3.  Finally, run
    ```bash
    python ./gui.py
    ```
    to enjoy !
